name: Build iOS .ipa

on:
  workflow_dispatch:
  push:
    branches: [master]

jobs:
  build:
    runs-on: macos-15

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm ci

      - name: Generate icons
        run: node scripts/gen-icons.js

      - name: Sync web to iOS
        run: npx cap sync

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '16.2'

      - name: Accept license
        run: |
          sudo xcodebuild -license accept 2>&1 || true
          # Download and install iOS platform for Xcode 16.2
          /Applications/Xcode_16.2.app/Contents/Developer/usr/bin/xcodebuild -downloadPlatform iOS 2>&1 || true

      - name: Build .app
        working-directory: ios/App
        run: |
          /Applications/Xcode_16.2.app/Contents/Developer/usr/bin/xcodebuild \
            -project App.xcodeproj \
            -scheme App \
            -configuration Release \
            -destination generic/platform=iOS \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            build 2>&1 | head -100

      - name: Package .ipa
        run: |
          mkdir -p Payload
          cp -r ios/App/build/Build/Products/Release-iphoneos/App.app Payload/
          zip -r SBA-Monitor.ipa Payload/
          rm -rf Payload

      - name: Upload .ipa
        uses: actions/upload-artifact@v4
        with:
          name: SBA-Monitor-ipa
          path: SBA-Monitor.ipa
